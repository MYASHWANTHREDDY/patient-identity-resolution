# Runtime dependencies, added as each phase actually uses them (no speculative pins).
# Phase 0: config loading.
PyYAML==6.0.2

# Phase 1: synthetic data generation.
Faker==33.1.0
pyarrow==18.1.0

# Phase 2: local backend + dbt.
duckdb==1.5.4
dbt-core==1.12.0
dbt-duckdb==1.10.1

# Phase 4: comparators, deterministic baseline, evaluation harness.
rapidfuzz==3.14.5
pandas==3.0.3
