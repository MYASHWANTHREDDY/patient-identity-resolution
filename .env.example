# Copy to .env and fill in. Never commit the real .env (see .gitignore).

# Which dataset tier to run against: ci | dev | scale. See config/matching.yml.
MDM_TIER=dev

# GCP — only required from Phase 10 onward (Terraform / BigQuery / Dataproc).
GCP_PROJECT=
GCP_REGION=
